# 安全策略

## 报告漏洞

请通过 GitHub 的私密渠道报告：本仓库 **Security** 标签页 → **Report a vulnerability**（Private vulnerability reporting）。请勿在公开 issue 中披露尚未修复的问题。

报告时请附上：复现步骤、受影响的版本、以及你观察到的实际行为（若涉及站点产物，请说明是 `build`、`selfcheck` 还是 `import-legacy` 路径）。本插件不联网，因此**不需要**提供任何本机路径之外的隐私信息。

## 支持范围

仅最新发布版本接受安全修复，当前为 `0.2.x`。

## 威胁模型边界（诚实声明）

本插件把"数据不出本机"作为硬约束，但边界要说清楚：

- **不联网、不上传**：生成与构建全程本地，不发任何网络请求，也不读写 `.git` 对象内容。唯一的可选例外是 `git log --oneline -15` 这类摘要命令。
- **产物净化与机检是有界保证，不是形式化证明**：站点页面的"零外链、零脚本注入"由构建期净化（SVG 走 XML 解析器白名单重序列化）与机检（外链扫描覆盖常见属性形态与 CSS 上下文）共同保障；外链扫描器是**有界启发式**，无法证明"任何输入都不可能生成外链"。引用机检只覆盖白名单扩展名。
- **不防御恶意 agent**：本插件约束的是**产物形态**，不是模型行为。`SKILL.md` 是协议约定文本，不是沙箱边界；agent 在本机以你的权限运行。
- **安装器**写入引擎的插件缓存与启用清单（`installed_plugins.json`、`setting.json`），新建配置文件用 `0600`，备份与原文件同权限；它不修改 `.git` 历史，也不触碰你的其他数据。
- **产物是本地静态文件**：`site/index.html` 自包含、可离线打开；把它分享出去是你的决定，插件不参与任何分发。
